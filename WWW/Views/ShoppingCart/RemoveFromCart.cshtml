@model Eduportal.Models.ViewModels.ActionShoppingCartViewModel
@{
    ViewBag.Title = "ShoppingCartItem Management";
    ViewBag.SubTitle = "Manage all your ShoppingCarts via this platform";
    ViewBag.Description = "Delete the ShoppingCartItem @Model.Name";
}
<div class="animated fadeIn">
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <i class="fa fa-align-justify"></i> ShoppingCart
            </div>
            <div class="card-block">
                <p>Do you wish remove the item  the cart  ?</p>
                <form asp-controller="ShoppingCart" asp-action="RemoveFromCart" method="post" asp-antiforgery="true">
                    <input asp-for="BaseEntity.Id" type="hidden">
                    <input asp-for="BaseEntity.Name" type="hidden">
                    <input asp-for="BaseEntity.Description" type="hidden">
                    <input asp-for="ActionType" type="hidden">
                    <button type="submit" class="btn btn-primary"></button>
                </form>
            </div>
        </div>
    </div>
    <!--/.col-->
</div>
<!--/.row-->
</div>
